import { Card, CardContent } from '@/components';

const ScheduleMonth = () => {
	return (
		<Card>
			<CardContent className="p-6">
				<div className="py-12 text-center">
					<h3 className="mb-2 text-xl font-semibold">Місячний вид</h3>
					<p className="text-muted-foreground">
						Функціонал місячного перегляду буде доступний у майбутньому
					</p>
				</div>
			</CardContent>
		</Card>
	);
};

export default ScheduleMonth;
